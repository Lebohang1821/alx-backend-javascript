// It define namespace called Subjects
namespace Subjects {
  // Define interface for teachers
  export interface Teacher {
    firstName: string; // Property for first name of teacher
    lastName: string;  // Property for last name of teacher
  }
}
